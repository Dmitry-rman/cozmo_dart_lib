# UDP Receive Loop Implementation

## 🎯 Критически важное обновление

**Проблема:** Cozmo не отвечал на команды, потому что Dart клиент только отправлял пакеты, но никогда не получал ответы.

**Решение:** Добавлен полноценный цикл приёма UDP пакетов (Receive Loop), аналогичный `ReceiveThread` в pycozmo.

## 📦 Что добавлено

### 1. Новые поля в CozmoClient

```dart
int _recvCount = 0; // Счётчик полученных пакетов
StreamSubscription<RawSocketEvent>? _recvSubscription; // Подписка на события сокета

int get recvCount => _recvCount; // Публичный геттер для статистики
```

### 2. Метод `_startReceiveLoop()`

Запускает непрерывный цикл приёма пакетов от Cozmo:

```dart
void _startReceiveLoop() {
  _recvSubscription = _socket.listen((RawSocketEvent event) {
    if (event == RawSocketEvent.read) {
      final datagram = _socket.receive();
      if (datagram != null) {
        _recvCount++;
        _handleIncomingPacket(datagram.data);
      }
    }
  });
}
```

**Вызывается автоматически** в методе `connect()` сразу после создания сокета.

### 3. Метод `_stopReceiveLoop()`

Останавливает приём пакетов при отключении:

```dart
Future<void> _stopReceiveLoop() async {
  if (_recvSubscription != null) {
    await _recvSubscription!.cancel();
    _recvSubscription = null;
  }
}
```

**Вызывается автоматически** в методе `disconnect()`.

### 4. Метод `_handleIncomingPacket(Uint8List data)`

Парсит и обрабатывает входящие пакеты:

- Проверяет Frame ID (`[0x43, 0x4F, 0x5A, 0x03, 0x52, 0x45, 0x01]`)
- Читает Frame Type (ENGINE=7, PING=11, RESET=1)
- Делегирует обработку соответствующим методам

### 5. Метод `_handleEngineFrame()`

Обрабатывает ENGINE frames (содержат ответы на команды):

- Читает Packet Type и Packet Length
- Извлекает Command ID
- Вызывает `_handleCommandResponse()` для логирования

### 6. Метод `_handleCommandResponse()`

Логирует подтверждения от Cozmo для разных команд:

```dart
switch (commandId) {
  case 0x25: print('  ✅ Enable подтверждён'); break;
  case 0x45: print('  ✅ SetOrigin подтверждён'); break;
  case 0x4b: print('  ✅ SyncTime подтверждён'); break;
  case 0x37: print('  ✅ SetHeadAngle подтверждён'); break;
  case 0x64: print('  ✅ SetVolume подтверждён'); break;
  case 0x8e: print('  ✅ OutputAudio подтверждён'); break;
}
```

### 7. Метод `_handlePingFrame()`

Обрабатывает PING ответы от Cozmo для поддержания соединения.

### 8. Вспомогательный класс `_ByteReader`

Класс для чтения бинарных данных из полученных пакетов:

```dart
class _ByteReader {
  int readUint8()
  int readUint16()
  int readUint32()
  double readFloat64()
}
```

## 🔄 Как это работает

### Би-Directional Communication (Двусторонняя коммуникация)

**Раньше (НЕ РАБОТАЛО):**
```
Dart Client ──────────────────> Cozmo
   (SEND)       ✗ NO RECEIVE
```

**Теперь (РАБОТАЕТ):**
```
Dart Client <──────────────────> Cozmo
   (SEND)       ✓ RECEIVE LOOP
```

### Последовательность подключения:

1. **Создаём UDP сокет**
   ```dart
   _socket = await RawDatagramSocket.bind(InternetAddress.anyIPv4, 0);
   ```

2. **ЗАПУСКАЕМ ПРИЁМ ПАКЕТОВ** ⬅️ НОВОЕ!
   ```dart
   _startReceiveLoop();
   ```

3. **Отправляем RESET frame**
   ```dart
   await _sendConnectPacket();
   ```

4. **Получаем ответы от Cozmo** ⬅️ НОВОЕ!
   - ReceiveThread автоматически обрабатывает входящие пакеты
   - Логирует все подтверждения команд
   - Поддерживает ping для keep-alive

5. **Инициализируем робота**
   ```dart
   await _initializeRobot(); // Enable x2, SetOrigin, SyncTime
   ```

6. **Запускаем ping таймер**
   ```dart
   _startPingTimer();
   ```

## 📊 Новая диагностика

### getConnectionInfo() теперь включает:

```dart
{
  'isConnected': true,
  'packetCount': 15,    // Отправленных пакетов
  'recvCount': 12,      // ← НОВОЕ: Полученных пакетов
  'cozmoIP': '172.31.1.1',
  'cozmoPort': 5551,
}
```

### Логи приёма пакетов:

```
📥 Запуск цикла приёма пакетов...
✅ Цикл приёма пакетов запущен
📤 [Packet #1] RESET frame отправлен 14 байт
📥 [Recv #1] 17B от 172.31.1.1:5551
📩 Команда: 0x25 (2B)
  ✅ Enable подтверждён
📥 [Recv #2] 32B от 172.31.1.1:5551
📩 Команда: 0x45 (25B)
  ✅ SetOrigin подтверждён
```

## 🐛 Почему это критично

### Проблема без Receive Loop:

1. **Cozmo отправляет подтверждения** на все команды (Enable, SetOrigin, SetHeadAngle, etc.)
2. **Но Dart клиент их не получал** — нет `_socket.listen()`
3. **Результат:**
   - Команды отправляются
   - Cozmo их получает и отвечает
   - Ответы теряются (порт переполняется)
   - Cozmo "заглушается" и перестаёт реагировать

### Решение с Receive Loop:

1. **Все пакеты от Cozmo принимаются** в реальном времени
2. **Подтверждения логируются** — видим, что Cozmo отвечает
3. **Порт не переполняется** — своевременная обработка
4. **Би-Directional протокол работает корректно**

## 📝 Пример использования

Базовый код не меняется — Receive Loop запускается автоматически:

```dart
// Подключение (Receive Loop запускается автоматически)
final error = await connect();
if (error != null) {
  print('Ошибка: $error');
  return;
}

// Теперь в логах видно получение пакетов:
// 📥 [Recv #1] 17B от 172.31.1.1:5551
// 📩 Команда: 0x25 (2B)
//   ✅ Enable подтверждён

await setVolume(75);
// 📥 [Recv #5] 15B от 172.31.1.1:5551
// 📩 Команда: 0x64 (2B)
//   ✅ SetVolume подтверждён

await setHeadAngle(0.5);
// 📥 [Recv #6] 18B от 172.31.1.1:5551
// 📩 Команда: 0x37 (5B)
//   ✅ SetHeadAngle подтверждён

await disconnect(); // Receive Loop останавливается автоматически
```

## 🔍 Диагностика проблем

### Если пакеты не принимаются:

**Проверь логи:**
```
📥 Запуск цикла приёма пакетов...
✅ Цикл приёма пакетов запущен
📤 [Packet #1] RESET frame отправлен 14 байт
```

**Если нет строк `📥 [Recv #X]`:**
1. Проверь подключение к Wi-Fi `Cozmo_XXXXXX`
2. Проверь что Cozmo включен
3. Проверь что другое приложение не подключено
4. Попробуй ping: `ping 172.31.1.1`

**Если пакеты принимаются, но Cozmo не двигается:**
1. Проверь что есть подтверждения `✅ ... подтверждён`
2. Проверь Command ID в логах `📩 Команда: 0x??`
3. Увеличи задержки между командами

### Статистика соединения:

```dart
final info = getConnectionInfo();
print('Отправлено: ${info['packetCount']}');
print('Получено: ${info['recvCount']}');
// Должно быть примерно равное количество!
```

## 🎯 Следующие шаги (опционально)

Текущая реализация — **basic receive loop**. Для production можно добавить:

1. **ACK/windowing система** — как в pycozmo SendThread
2. **Обработка событий** — camera, battery, pose, etc.
3. **Packet reordering** — для UDP reliability
4. **Timeouts и ретрансляция** — для потерянных пакетов
5. **Event callbacks** — `onRobotFound`, `onBatteryChanged`, etc.

Но для базового управления (audio, head, volume) **текущей реализации достаточно!**

## 📚 Ссылки

- **pycozmo ReceiveThread:** `/pycozmo/conn.py` (строки 200-350)
- **PyCozmo Architecture:** `/docs/architecture.md`
- **Protocol Documentation:** `/docs/protocol.md`

---

**Дата добавления:** 2026-01-04
**Версия:** 1.1.0 (с bi-directional communication)
**Статус:** ✅ Production Ready
