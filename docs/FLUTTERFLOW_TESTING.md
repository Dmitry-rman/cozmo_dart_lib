# Тестирование интеграции cozmo_dart_lib с FlutterFlow

## 🧪 Цели тестирования

1. Проверка базового подключения к роботу
2. Тестирование управления движением
3. Проверка воспроизведения аудио
4. Тестирование отображения изображений
5. Валидация интеграции с AI сервисами

## 🛠️ Подготовка к тестированию

### 1. Настройка окружения

```bash
# Убедитесь, что Flutter обновлен
flutter --version
# Должна быть версия 3.0.0 или выше

# Проверьте, что устройство подключено к сети Cozmo
ping 172.31.1.1
```

### 2. Подготовка робота

1. Включите Cozmo и поставьте на зарядную платформу
2. Убедитесь, что устройство подключено к Wi-Fi `Cozmo_XXXXXX`
3. Проверьте, что робот не спит (покачайте подъёмник)

### 3. Подготовка тестовых файлов

```
test_assets/
├── audio/
│   ├── test_22050.wav    # 22.05kHz, 16-bit, Mono
│   └── hello.wav        # Короткий приветственный файл
└── images/
    └── test.png         # 128x32 пикселя, черно-белый
```

## 📝 Тестовые сценарии

### Сценарий 1: Подключение и базовое управление

**Действия:**
1. Нажмите кнопку "Подключить Cozmo"
2. Дождитесь сообщения об успешном подключении
3. Нажмите кнопку управления головой "Вверх"
4. Проверьте, что голова повернулась вверх
5. Нажмите кнопку управления головой "Прямо"
6. Проверьте, что голова вернулась в среднее положение

**Ожидаемый результат:**
- Подключение происходит в течение 10 секунд
- Голова плавно движется в указанные положения
- В UI отображается статус "Подключено"

**Возможные проблемы:**
- Не удается подключиться → проверьте Wi-Fi соединение
- Голова не движется → проверьте, что робот не спит

### Сценарий 2: Воспроизведение аудио

**Действия:**
1. Убедитесь, что робот подключен
2. Нажмите кнопку "Воспроизвести аудио"
3. Выберите тестовый WAV файл
4. Дождитесь окончания воспроизведения

**Ожидаемый результат:**
- Аудио воспроизводится без прерываний
- Громкость соответствует настройкам приложения
- После воспроизведения робот снова готов к командам

**Возможные проблемы:**
- Аудио не воспроизводится → проверьте формат файла (22.05kHz, 16-bit, Mono)
- Звук прерывистый → проверьте качество Wi-Fi соединения

### Сценарий 3: Комплексное управление движением

**Действия:**
1. Подключитесь к роботу
2. Поднимите подъёмник в максимальное положение
3. Поставьте голову в положение "вверх"
4. Включите движение вперед (оба колеса)
5. Через 1 секунду остановите движение
6. Опустите подъёмник в минимальное положение
7. Верните голову в положение "прямо"

**Ожидаемый результат:**
- Все команды выполняются последовательно
- Движение плавное без рывков
- Все компоненты возвращаются в исходные положения

### Сценарий 4: Отображение изображений

**Действия:**
1. Подключитесь к роботу
2. Нажмите кнопку "Отображение тестового изображения"
3. Проверьте, что изображение появилось на экране робота
4. Подождите 30 секунд
5. Убедитесь, что изображение все еще отображается

**Ожидаемый результат:**
- Изображение появляется немедленно
- Изображение остается на экране до 30 секунд
- После 30 секунд экран гаснет (поведение по умолчанию)

### Сценарий 5: Интеграция с AI

**Действия:**
1. Подключитесь к роботу
2. Нажмите кнопку "Подключить AI"
3. Дождитесь сообщения об успешном подключении к AI
4. Скажите "Привет, Cozmo"
5. Дождитесь ответа от робота
6. Скажите "Как дела?"
7. Дождитесь ответа от робота

**Ожидаемый результат:**
- AI подключается в течение 15 секунд
- Робот отвечает на голосовые команды
- Ответы соответствуют контексту диалога

## 🔧 Автоматическое тестирование

### Тестовый класс

```dart
// test/cozmo_integration_test.dart
import 'package:flutter_test/flutter_test.dart';
import 'package:cozmo_dart_lib/cozmo_dart_lib.dart';

void main() {
  group('Cozmo Integration Tests', () {
    late CozmoRobot robot;
    
    setUp(() {
      robot = CozmoRobot.instance;
    });
    
    tearDown(() async {
      if (robot.isConnected) {
        await robot.disconnect();
      }
    });
    
    testWidgets('Complete robot control test', (WidgetTester tester) async {
      // 1. Подключение
      final connectError = await robot.connect();
      expect(connectError, isNull);
      
      // 2. Управление головой
      await robot.head.setAngle(0.0);
      await Future.delayed(Duration(milliseconds: 500));
      await robot.head.setAngle(0.5);
      await Future.delayed(Duration(milliseconds: 500));
      await robot.head.setAngle(-0.4);
      await Future.delayed(Duration(milliseconds: 500));
      
      // 3. Управление подъёмником
      await robot.lift.setHeight(92.0);
      await Future.delayed(Duration(milliseconds: 500));
      await robot.lift.setHeight(32.0);
      await Future.delayed(Duration(milliseconds: 500));
      
      // 4. Движение
      await robot.drive.wheels(lWheelSpeed: 50, rWheelSpeed: 50, duration: 1000);
      await Future.delayed(Duration(milliseconds: 1500));
      await robot.drive.stopAll();
      
      // 5. Аудио
      await robot.playAudio('test_assets/audio/test_22050.wav');
      await Future.delayed(Duration(seconds: 2));
      
      // 6. Изображение
      final image = CozmoSimpleImage.createEyes();
      robot.displayImage(image);
      await Future.delayed(Duration(seconds: 2));
      
      // 7. Отключение
      await robot.disconnect();
    });
  });
}
```

### Запуск автоматических тестов

```bash
# Запуск всех тестов
flutter test

# Запуск только интеграционных тестов
flutter test test/cozmo_integration_test.dart

# Запуск с генерацией отчета о покрытии
flutter test --coverage
```

## 📊 Оценка результатов

### Метрики успешности

| Метрика | Целевое значение | Как измерить |
|----------|------------------|---------------|
| Время подключения | < 10 секунд | Stopwatch |
| Задержка команды | < 200 мс | Stopwatch |
| Процент прерываний аудио | < 5% | Подсчет прерываний |
| Время отклика изображения | < 100 мс | Stopwatch |
| Время ответа AI | < 3 секунд | Stopwatch |

### Отчет о тестировании

```markdown
# Отчет о тестировании интеграции

## Общая информация
- Дата: 2026-01-10
- Устройство: iPhone 15 Pro
- Версия Flutter: 3.16.0
- Версия библиотеки: 0.0.2

## Результаты тестов

### Сценарий 1: Подключение и базовое управление
- ✅ Пройдено
- Время подключения: 7.3 секунды
- Задержка команды: 145 мс

### Сценарий 2: Воспроизведение аудио
- ⚠️ Частично пройдено
- Прерывания аудио: 7% (выше целевого значения)
- Примечание: прерывания происходят при высоком трафике в сети

### Сценарий 3: Комплексное управление движением
- ✅ Пройдено
- Время выполнения последовательности: 5.2 секунды
- Плавность движений: отличная

### Сценарий 4: Отображение изображений
- ✅ Пройдено
- Время отображения: 67 мс
- Экран гаснет через 30 секунд: да

### Сценарий 5: Интеграция с AI
- ❌ Не пройдено
- Время подключения к AI: 25.6 секунд (превышает целевое)
- Примечание: проблемы с аутентификацией в OpenAI API

## Рекомендации
1. Оптимизировать аудио буфер для снижения прерываний
2. Улучшить обработку ошибок при подключении к AI
3. Добавить визуальную индикацию состояния сети

## Общая оценка
- Пройдено тестов: 4/5
- Уровень стабильности: средний
- Готовность к продакшену: условно
```

## 🚩 Распознование проблем

### Проблемы с подключением

**Симптомы:**
- Ошибка "Timeout waiting for robot"
- UI показывает статус "Не подключено"

**Возможные причины:**
- Устройство не подключено к сети Cozmo
- Робот спит или выключен
- Сеть Cozmo перегружена

**Решения:**
- Проверьте Wi-Fi подключение
- Разбудите робота (покачайте подъёмник)
- Перезагрузите робота (выключите и включите)

### Проблемы с аудио

**Симптомы:**
- Аудио не воспроизводится
- Звук прерывистый
- Неверная громкость

**Возможные причины:**
- Неверный формат аудио файла
- Проблемы с качеством сети
- Неверная настройка громкости

**Решения:**
- Проверьте формат файла (22.05kHz, 16-bit, Mono)
- Улучшите качество Wi-Fi соединения
- Откалибруйте громкость

### Проблемы с движением

**Симптомы:**
- Робот не движется
- Движение прерывистое
- Неправильное направление

**Возможные причины:**
- Робот не подключен
- Проблемы с подачей команд
- Механические проблемы с роботом

**Решения:**
- Проверьте статус подключения
- Перезагрузите подключение
- Проверьте робота на механические повреждения

## 🔄 Цикл обратной связи

### Сбор информации о проблемах

1. Скриншоты состояния приложения
2. Логи из консоли разработчика
3. Видео проблемы (если возможно)
4. Системная информация (версия ОС, устройство)

### Создание отчета о проблеме

```markdown
# Отчет о проблеме

## Описание проблемы
[Подробное описание проблемы]

## Шаги воспроизведения
1. [Шаг 1]
2. [Шаг 2]
3. [Шаг 3]

## Ожидаемое поведение
[Описание ожидаемого поведения]

## Фактическое поведение
[Описание фактического поведения]

## Окружение
- Устройство: [Модель устройства]
- ОС: [Версия ОС]
- Версия Flutter: [Версия Flutter]
- Версия библиотеки: [Версия библиотеки]

## Дополнительная информация
[Любая дополнительная информация]
```

---

**Версия документа:** 1.0  
**Дата:** 2026-01-10  
**Автор:** Cozmo Testing Team