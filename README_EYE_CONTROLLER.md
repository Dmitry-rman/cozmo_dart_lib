# EyeAnimationController для cozmo_dart_lib

👁 **Новый компонент для управления анимацией глаз робота Cozmo**

EyeAnimationController - это компонент для управления анимацией глаз робота Cozmo, который автоматически активируется при подключении к роботу. Компонент адаптирует паттерны анимации из pycozmo для Dart/Flutter архитектуры и обеспечивает плавную, выразительную анимацию глаз без блокирования основного потока приложения.

## 🎯 Возможности

### Типы анимаций

1. **Ожидание (Wandering)** - плавное движение глаз при простое
2. **Моргание (Blink)** - однократное или циклическое моргание
3. **Эмоциональные выражения** - счастливые, грустные, удивленные, думающие глаза
4. **Сон/Пробуждение** - постепенное закрытие и открытие век

### Преимущества

- 🔄 **Автоматическая активация** при подключении
- ⚡ **Асинхронное выполнение** без блокирования UI
- 🎨 **Плавные переходы** между состояниями
- 🎮 **Настраиваемые параметры** анимации
- 🧠 **Расширяемость** для добавления новых типов анимаций

## 🏗️ Архитектура

### Классы

```dart
enum EyeAnimationType {
  none,
  blink,
  blinkLoop,
  wander,
  sleep,
  wakeup,
  happy,
  sad,
  surprised,
  thinking,
}

class EyeAnimationController {
  // Основные поля и методы
}
```

### Интеграция с CozmoRobot

```dart
class CozmoRobot {
  late final EyeAnimationController eyeController; // 🆕 Добавлен
  
  CozmoRobot._internal() {
    // Инициализация
    eyeController = EyeAnimationController(_client, face, animController);
    
    // Автоматическая активация при подключении
    // ...
  }
}
```

## 🎮 Использование

### Базовое использование

```dart
final robot = CozmoRobot.instance;
await robot.connect(); // Автоматически активирует анимацию глаз

// Моргание
robot.eyeController.blink();

// Счастливые глаза
robot.eyeController.setHappy();

// Циклическое моргание
robot.eyeController.startBlinkLoop();
robot.eyeController.stopBlinkLoop();
```

### Интеграция с FlutterFlow

```dart
// Кастомное действие
Future robotEyeBlink() async {
  final robot = CozmoRobot.instance;
  robot.eyeController.blink();
}

// UI компонент
FFButtonWidget(
  onPressed: () async {
    await actions.robotEyeBlink();
  },
  text: 'Моргнуть',
),
```

## 📋 Тестирование

```dart
// Автоматическая активация при подключении
test('Eyes animate on connect', () async {
  final robot = CozmoRobot.instance;
  await robot.connect();
  expect(robot.eyeController.isActive, true);
  expect(robot.eyeController.currentAnimation, EyeAnimationType.wander);
});

// Моргание
test('Blink animation', () async {
  final robot = CozmoRobot.instance;
  await robot.connect();
  
  // Проверяем состояние до моргания
  expect(robot.eyeController.currentAnimation, EyeAnimationType.wander);
  
  // Выполняем моргание
  robot.eyeController.blink();
  
  // Проверяем состояние после моргания
  await Future.delayed(Duration(milliseconds: 100));
  expect(robot.eyeController.currentAnimation, EyeAnimationType.wander);
});
```

## 🚀 Дорожная карта

### Версия 1.0.0 (Текущая)

- ✅ Базовые анимации (моргание, ожидание)
- ✅ Эмоциональные выражения
- ✅ Автоматическая активация
- ✅ Интеграция с FlutterFlow

### Версия 1.1.0 (Планируемая)

- ✅ Комбинированные анимации (моргание + эмоция)
- ✅ Параметризованные анимации
- ✅ Контекстуальные анимации (реакция на события)
- ✅ UI компоненты для управления
- ✅ Расширенная документация

### Версия 2.0.0 (Будущая)

- ✅ Случайные анимации
- ✅ Адаптивные параметры (на основе статистики)
- ✅ Сохранение/загрузка преднастроек анимаций
- ✅ Интеграция с AI для эмоционального анализа

## 📚 Примеры

### 1. Анимация пробуждения

```dart
// Устанавливаем спящие глаза
robot.eyeController.setSad();

// Запускаем анимацию пробуждения
robot.eyeController.startAnimation(EyeAnimationType.wakeup);

// Ждем окончания анимации
await Future.delayed(Duration(seconds: 1));

// Проверяем результат
expect(robot.eyeController.currentAnimation, EyeAnimationType.wander);
```

### 2. Комбинированная анимация

```dart
// Устанавливаем думающие глаза
robot.eyeController.setThinking();

// Запускаем циклическое моргание
robot.eyeController.startBlinkLoop();

// Ждем некоторое время
await Future.delayed(Duration(seconds: 3));

// Устанавливаем счастливые глаза
robot.eyeController.setHappy();

// Останавливаем циклическое моргание
robot.eyeController.stopBlinkLoop();
```

## 📝 Советы по использованию

1. **Начните с простого** - используйте базовые анимации (моргание, ожидание)
2. **Добавляйте по одному** - не комбинируйте слишком много эффектов сразу
3. **Используйте задержки** - добавляйте задержки между анимациями для более естественного вида
4. **Экспериментируйте** - пробуйте разные параметры для нахождения оптимальных значений
5. **Следите за состоянием** - проверяйте, что анимации работают корректно

## 🔗 Конфигурация

### Параметры по умолчанию

```dart
// Интервал моргания (4 секунды)
Duration _blinkInterval = Duration(seconds: 4);

// Интервал ожидания (2 секунды)
Duration _wanderInterval = Duration(milliseconds: 2000);

// Амплитуда движения глаз при ожидании (5% от ширины)
double _wanderAmplitude = 0.05;
```

### Настройка параметров

```dart
// Настройка интервала моргания
robot.eyeController.setBlinkInterval(Duration(seconds: 3));

// Настройка амплитуды движения
robot.eyeController.setWanderAmplitude(0.08);

// Перезапуск анимации после изменения параметров
robot.eyeController.stopAnimation();
robot.eyeController.startAnimation(EyeAnimationType.wander);
```

---

**Версия:** 1.0.0  
**Дата:** 2026-01-10  
**Автор:** Cozmo Development Team